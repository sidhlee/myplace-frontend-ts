import React from 'react';
import styled from 'styled-components';

const StyledNewPlace = styled.div``;
const NewPlace: React.FC = (props) => {
  return <StyledNewPlace>NewPlace</StyledNewPlace>;
};

export default NewPlace;
