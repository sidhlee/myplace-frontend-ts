import React from 'react';
import styled from 'styled-components';

const StyledUserItem = styled.div``;
const UserItem: React.FC = (props) => {
  return <StyledUserItem>UserItem</StyledUserItem>;
};

export default UserItem;
